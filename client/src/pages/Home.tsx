/* ============================================================
   Home — 「宵桜の金箔絵巻」集客サイトのエントリーページ
   デザイン: 公式ポスターを基準に、来場判断・周遊・アクセスを最優先に配置。
   ============================================================ */
import EventHome from "@/components/EventHome";

export default function Home() {
  return <EventHome />;
}
